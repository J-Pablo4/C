#include <stdio.h>

int main(void)
{
    /* int contador = 1;
    int acumulador = 0;
    int n =1;
    float total;
    while (n !=0)
    {
        printf("Calificacion %d: ", contador);
        scanf("%d", &n);
        if (n!=0 && n<=10)
        {
            acumulador += n;
            if (n!=0)
            {
                contador ++;
            }
        }
        else if (n>10)
        {
            printf("Calificacion %d: ", contador);
            scanf("%d", &n);
        }
        
    }
    contador --;
    total = (float)acumulador/contador;
    printf("Promedio: %.1f", total);   */

    /* int c=1,c2,m;
    while (c <= 5)
    {
        c2 = 1;
        printf("Tabla del %d\n",c);
        while (c2<=10)
        {
            m = c*c2;
            printf("%d x %d = %d\n",c,c2,m);
            c2++;
        }
        c++;
    }
      */
    
    /* for (int i = 1; i <= 100; i++)
    {
        printf("%d, ", i);
    }

    for (int i = 100; i >= 1; i--)
    {
        printf("%d, ", i);
    }  */
    
    return 0;
}