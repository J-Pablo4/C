#include <stdio.h>

int main (void){
    for ( int x = 1; x <= 5; x++)
    {
        for (int y = 1; y <= 10 ; y++)
        {
            printf("%d x %d = %d\n", x, y, (x*y));
        }
        
    } 

    /* int M;
    printf("Escribe un numero: ");
    scanf("%d", &M); */

    /* for ( int i = 0; i < M; i +=2)
    {
        printf("%d\n", i);
    } */

    /* for (int i = 1; i < M; i++)
    {
        if (i&1)
        {
            printf("%d\n", i);
        }
        
    } */

    /* int N;
    printf("Escribe un numero: ");
    scanf("%d", &N);
    while (N>0)
    {
        printf("%d\n", contador);
        contador += 2;
        N --;
    } */
    
    
    
    return 0;
}